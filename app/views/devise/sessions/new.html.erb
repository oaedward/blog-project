<%= form_for(resource, as: resource_name, url: session_path(resource_name), data: {turbo: false}) do |f| %>
	<div class="flex min-h-full items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
		<div class="w-full max-w-md space-y-8">
			<div>
				<h2 class="mt-6 text-center text-3xl font-bold tracking-tight text-gray-900 dark:text-gray-50">Sign in to your account</h2>
				<p class="mt-2 text-center text-sm text-gray-600">
					Exclusively for content creators
				</p>
			</div>
			<div class="-space-y-px rounded-md shadow-sm">
				<div>
					<%= f.label :email, class:"sr-only"  %>
					<%= f.email_field :email, autofocus: true, autocomplete: "email", class:"relative block w-full appearance-none rounded-none px-3 py-2 text-gray-900 placeholder-gray-500 focus:z-10 focus:border-gray-500 focus:outline-none focus:ring-gray-500 sm:text-sm mb-3", placeholder:"Email address" %>
				</div>
				<div>
					<%= f.label :password, class:"sr-only" %>
					<%= f.password_field :password, autocomplete: "current-password", class:"relative block w-full appearance-none rounded-none px-3 py-2 text-gray-900 placeholder-gray-500 focus:z-10 focus:border-gray-500 focus:outline-none focus:ring-gray-500 sm:text-sm", placeholder:"Password"  %>
				</div>
			</div>
			<div class="flex items-center justify-between">
				<% if devise_mapping.rememberable? %>
					<div class="flex items-center">
						<%= f.check_box :remember_me, class:"h-4 w-4 rounded border-gray-300 text-gray-600 focus:ring-gray-500"  %>
						<%= f.label :remember_me, class:"ml-2 block text-sm dark:text-gray-50 text-gray-900" %>
					</div>
				<% end %>
			</div>
			<div>
				<%= f.submit "Sign in", class:"group relative flex w-full justify-center rounded-md border border-transparent bg-gray-900 py-2 px-4 text-sm font-medium text-white hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2" %>
			</div>
		</div>
	</div>
<% end %>
<%= render "devise/shared/links" %>
